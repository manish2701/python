import turtle

t = turtle.Turtle()

# move turtle to:
#t.goto(0,-100)

#take the turtle above from the canvas and prevent it from drawing 
#t.up() and t.down()

t.up()
t.goto(0,-100)
t.down()

t.circle(100)

turtle.done()